{
  "name": "gtfs-express-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end smoke tests for GTFS Express (Playwright). Boots the real API and serves the built frontend.",
  "license": "AGPL-3.0-only",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.1",
    "serve": "^14.2.4"
  }
}
